name: feature_flags_toggly_sqlite_example
description: Example for feature_flags_toggly_sqlite.
publish_to: 'none'

environment:
  sdk: '>=2.18.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  feature_flags_toggly_sqlite:
    path: ../

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
