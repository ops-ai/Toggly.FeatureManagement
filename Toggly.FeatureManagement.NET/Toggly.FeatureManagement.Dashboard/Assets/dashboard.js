document.addEventListener("DOMContentLoaded",()=>{document.querySelectorAll("form[data-unsaved-warning]").forEach(form=>{let dirty=false;form.querySelectorAll("input,textarea,select").forEach(input=>input.addEventListener("change",()=>dirty=true));form.addEventListener("submit",()=>dirty=false);window.addEventListener("beforeunload",event=>{if(dirty){event.preventDefault();event.returnValue="";}});});});
