{
  "name": "@ops-ai/nextjs-toggly-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Feature flags for Next.js - monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r test:coverage",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean",
    "dev:prepare": "pnpm -r dev:prepare"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@9.15.4",
  "devDependencies": {
    "@types/ws": "^8.0.0",
    "ws": "^8.0.0"
  }
}
